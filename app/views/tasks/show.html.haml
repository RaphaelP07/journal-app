%p{:style => "color: green"}= notice
= render @task
%div
  = link_to "Edit this task", edit_category_task_path(@task)
  |
  \#{link_to "Back to category tasks", category_tasks_path}
  
  \#{button_to "Destroy this task", @task, method: :delete}
