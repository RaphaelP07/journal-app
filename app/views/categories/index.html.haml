%p{:style => "color: green"}= notice
%h1 Categories
#categories
  - @categories.each do |category|
    = render category
    %p
      = link_to "Show this category", category
= button_to "New category", new_category_path, method: :get
%br
= button_to "Show Today's Tasks", today_tasks_path, method: :get
