-# %p{:style => "color: green"}= notice
.show-container
  = render @category
  %div
    = link_to "Edit this category", edit_category_path(@category)
    |
    \#{link_to "Show tasks", "/categories/#{@category.id}/tasks"}
    |
    \#{link_to "Back to categories", categories_path}

